source 'http://rubygems.org'

gem 'rails', '~> 3.0.5'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'mysql2'
gem 'jquery-rails'

gem "escape_utils" # => 清除warning: regexp match /.../n against to UTF-8 string

# gem 'authlogic'
gem 'whenever', :require => false # => 定时任务
gem 'devise' # => 会员权限认证
gem 'dynamic_form' # => 表单错误显示

#gem 'gchart', '~>1.0.0' # google chart
gem 'gchart', :git => 'git://github.com/GutenLinux/gchart.git'

gem 'meta_search' # => 搜索
gem "paperclip", "~> 2.3" # => 文件上传
gem 'kaminari' # => 分页
#gem 'exception_notification_rails3', :require => 'exception_notifier' # => 邮件发送错误信息

#gem 'rails_best_practices'

group :development do
  gem 'to_lang'
  gem 'ya2yaml'
end

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger (ruby-debug for Ruby 1.8.7+, ruby-debug19 for Ruby 1.9.2+)
# gem 'ruby-debug'
# gem 'ruby-debug19'

# Bundle the extra gems:
# gem 'bj'
# gem 'nokogiri'
# gem 'sqlite3-ruby', :require => 'sqlite3'
# gem 'aws-s3', :require => 'aws/s3'

# Bundle gems for the local environment. Make sure to
# put test-only gems in this group so their generators
# and rake tasks are available in development mode:
# group :development, :test do
#   gem 'webrat'
# end
